# Ревью Задания №7: Структурирование End-to-End Приложений

**Дата проверки:** 2026-02-28
**Версия решения:** v1
**Проверяющий:** Claude Code

---

## Общая оценка: 85/100 баллов + 15 бонусных баллов

Хорошая работа! Студент демонстрирует понимание ключевых концепций: структурированные JSON-запросы, обработка ошибок, батчинг, retry-логика и подсчёт токенов. Код хорошо организован, все части реализованы. Основные замечания касаются несоответствия формата вывода требованиям задания, бага с захардкоженным «XXX» в задаче 5.2, и неполной реализации JSON-схемы в задаче 3.2.

---

## Детальная оценка по частям

### Часть 1: Структурированный JSON-ответ — 16/20 баллов

#### ✅ Что сделано правильно:

**Задача 1.1:**

1. **Корректный запрос с `response_format`:**
```python
response_format={'type':'json_object'}
```
Формат задан верно.

2. **Чёткий промпт с явным указанием структуры JSON:**
```python
prompt = '''
Сгенерируй список из 4 ИТ профессий в формате json. В выводе каждой профессии должны быть поля:
- title: "Название должности"
- description: "Краткое описание (1 предложение)
- key_skills: ['навык_1','навык_2','навык_3']
- avg_salary_usd: средняя зарплата в долларах
'''
```
Структура описана понятно, модель вернула корректные данные.

3. **Парсинг и сортировка через pandas:**
```python
df = pd.DataFrame(data)
sorted_df = df.sort_values(by=['avg_salary_usd'], ascending=False)
```
Функционально верно, сортировка работает.

**Задача 1.2:**

4. **Использование XML-подобных тегов для структуры промпта:**
```python
<Начало блока структуры ответа>
...
<Конец блока структуры ответа>
```
Это лучшая практика при передаче сложных инструкций.

5. **Инструкция обработки отсутствующих данных:**
```python
Если каких-то данных нет в тексте не выдумывай их а напиши "нет данных"
```
Грамотный подход для production-сценариев.

6. **Все поля извлечены корректно** — вывод содержит company, position, location, experience_years, required_skills, optional_skills, salary_min, salary_max, work_format, contact.

#### ⚠️ Что нужно исправить:

**Задача 1.1 — несоответствие формата вывода (–2 балла):**

Ожидаемый вывод из задания:
```
=== IT-ПРОФЕССИИ ПО ЗАРПЛАТЕ ===
1. [Название] — $XXX,XXX
2. [Название] — $XXX,XXX
```

Фактический вывод:
```
                title  avg_salary_usd
3     DevOps Engineer          120000
1      Data Scientist          115000
0  Software Developer          100000
2    Network Engineer           95000
```

DataFrame выводит данные в правильном порядке, но формат не соответствует требованию. Нужно добавить заголовок и пронумерованный список:

```python
print("=== IT-ПРОФЕССИИ ПО ЗАРПЛАТЕ ===")
for i, (_, row) in enumerate(sorted_df.iterrows(), 1):
    print(f"{i}. {row['title']} — ${row['avg_salary_usd']:,}")
```

**Задача 1.2 — сырой JSON вместо форматированного вывода (–2 балла):**

Используется:
```python
print(response.choices[0].message.content)
```

Ожидалось:
```
Компания: DataVision
Должность: Старший аналитик данных
Город: Москва
Опыт (лет): 3
...
```

Нужно парсить JSON и выводить каждое поле с меткой:
```python
result = json.loads(response.choices[0].message.content)
labels = {
    'company': 'Компания',
    'position': 'Должность',
    'location': 'Город',
    'experience_years': 'Опыт (лет)',
    'required_skills': 'Обязательные навыки',
    'optional_skills': 'Дополнительные навыки',
    'salary_min': 'Зарплата от',
    'salary_max': 'Зарплата до',
    'work_format': 'Формат работы',
    'contact': 'Контакт'
}
for key, label in labels.items():
    value = result[key]
    if isinstance(value, list):
        value = ', '.join(value)
    print(f"{label}: {value}")
```

---

### Часть 2: Обработка ошибок — 23/25 баллов

#### ✅ Что сделано правильно:

**Задача 2.1:**

1. **Все четыре типа ошибок обрабатываются раздельно:**
```python
except openai.AuthenticationError as e:
    print(f'[AUTH ERROR] Неверный API-ключ.')
except openai.RateLimitError as e:
    print(f'[RATE LIMIT] Превышен лимит запросов. Попробуйте позже')
except openai.BadRequestError as e:
    print(f'[BAD REQUEST] Некорректный запрос: {e}')
except Exception as e:
    print(f'[ERROR] Неизвестная ошибка: {e}')
```

2. **Все три тестовых сценария проверены:**

Тест 1 (корректный запрос): ✅ Ответ получен
Тест 2 (неверный ключ): ✅ `[AUTH ERROR] Неверный API-ключ.`
Тест 3 (роль «god»): ✅ `[BAD REQUEST] Некорректный запрос: Error code: 400 - ...`

**Задача 2.2:**

3. **Логирование до и после запроса:**
```python
print(f"[LOG] Отправка запроса... (символов в промпте: {len(prompt)})")
print(f'[LOG] Ответ получен. Токенов использовано: {response.usage.total_tokens}')
```

4. **Применение `clean_response()` для очистки от тегов `<think>`** — хорошее решение для работы с reasoning-моделями.

#### ⚠️ Что нужно исправить:

**Задача 2.1 — нет fallback на `GROQ_TOKEN` (–1 балл):**

В шаблоне задания был предусмотрен fallback:
```python
# Шаблон из задания:
api_key=api_key or os.getenv("GROQ_TOKEN")
```

В решении:
```python
# Фактически:
client_safe = OpenAI(
    api_key=api_key,  # Если None — клиент не инициализируется без OPENAI_API_KEY
    base_url=os.getenv('BASE_URL_GROQ')
)
```

Это не проявилось в тестах, так как Тест 1 явно передаёт `api_key=os.getenv('GROQ_TOKEN')`, но нарушает сигнатуру функции: вызов `safe_request("prompt")` без api_key упадёт с ошибкой об отсутствии ключа, а не вернёт ответ.

**Задача 2.1 — `clean_response()` не применяется (–1 балл):**

В выводе Теста 1 видны теги `<think>`:
```
<think>
Okay, the user said "Hi"...
</think>

Hello! How can I help you today?
```

Функция `clean_response()` определена в setup-ячейке, но в `safe_request()` не используется. В `logged_request()` это исправлено — нужно применить ту же логику.

---

### Часть 3: Батчинг запросов — 16/20 баллов

#### ✅ Что сделано правильно:

**Задача 3.1 — полное и отличное выполнение:**

1. **Оба варианта реализованы** с корректными системными промптами
2. **Токены суммируются** для каждого варианта
3. **Сравнение выведено** в требуемом формате:
```
=== СРАВНЕНИЕ ===
Батчинг сэкономил 1064 токенов (65.2%)
```
4. **Вывод соответствует ожидаемому результату** ✅

#### ⚠️ Что нужно исправить:

**Задача 3.2 — неправильные поля JSON и отсутствие названий устройств (–4 балла):**

Задание требует поля: `name`, `brand`, `os`, `price_usd`.

Системный промпт студента:
```python
system_prompt = '''Тебе передают список смартфонов. Предоставь ответ в виде json. Поля в json: бренд, ОС и примерная цена в USD.'''
```

Результат:
```
   бренд       ОС  примерная цена в USD
0    Apple      iOS                  1000
1  Samsung  Android                   700
```

Проблемы:
- **Поле `name` (название устройства) отсутствует** — главный идентификатор продукта потерян (–2)
- **Поля на русском** вместо `brand`, `os`, `price_usd` (–1)
- **Формат таблицы** отличается от ожидаемого с разделителями `|` (–1)

Правильный системный промпт:
```python
system_prompt = '''Тебе передают список смартфонов. Предоставь ответ в виде json со списком объектов.
Структура каждого объекта:
{
    "name": "полное название устройства",
    "brand": "бренд",
    "os": "операционная система",
    "price_usd": примерная цена в USD (число)
}'''
```

---

### Часть 4: Retry-логика с Tenacity — 18/20 баллов

#### ✅ Что сделано правильно:

**Задача 4.1 — корректная конфигурация `@retry`:**

```python
@retry(
    retry=retry_if_exception_type((openai.RateLimitError, openai.APIConnectionError)),
    stop=stop_after_attempt(4),
    wait=wait_exponential(min=1, max=30),
    before_sleep=log_retry
)
```

Все параметры соответствуют требованиям:
- ✅ Типы ошибок для повтора: `RateLimitError`, `APIConnectionError`
- ✅ Максимум 4 попытки
- ✅ Экспоненциальный откат от 1 до 30 секунд
- ✅ Логирование перед каждой повторной попыткой

**Задача 4.2 — правильное объединение `@retry` и `try/except`:**

Использование `@retry` поверх `try/except` — правильный паттерн: retry срабатывает только на сетевые ошибки, остальные перехватываются внутри функции.

#### ⚠️ Что нужно исправить:

**Задача 4.2 — баг с `api_key=None` в `production_request` (–2 балла):**

```python
def production_request(prompt:str, api_key:str=None, role:str='user'):
    try:
        client = OpenAI(
            api_key=api_key,  # При api_key=None → OpenAI ищет OPENAI_API_KEY
            base_url=os.getenv('BASE_URL_GROQ')
        )
```

Тест 2 (пустой промпт без ключа) завершился ошибкой:
```
[ERROR] Неизвестная ошибка: The api_key client option must be set either by passing api_key to the client or by setting the OPENAI_API_KEY environment variable
```

Ошибка некорректная — вместо проверки пустого промпта (`BadRequestError`) получили ошибку конфигурации. Исправление:
```python
client = OpenAI(
    api_key=api_key or os.getenv('GROQ_TOKEN'),
    base_url=os.getenv('BASE_URL_GROQ')
)
```

---

### Часть 5: Подсчёт токенов — 12/15 баллов

#### ✅ Что сделано правильно:

**Задача 5.1 — корректный подсчёт токенов:**

```python
encoder = tiktoken.encoding_for_model('gpt-4o-mini')
tokens = len(encoder.encode(prompts[prompt]))
```

Результаты точные:

| Промпт   | Символов | Токенов | Символов/токен |
|----------|----------|---------|----------------|
| Короткий | 7        | 3       | 2.33           |
| Средний  | 92       | 26      | 3.54           |
| Длинный  | 276      | 68      | 4.06           |

Совпадает с ожидаемыми значениями ✅

#### ⚠️ Что нужно исправить:

**Задача 5.1 — формат вывода (–1 балл):**

Используется pandas DataFrame вместо форматированного print. Функционально корректно, но задание предполагало использование f-string форматирования:
```python
print(f"{'Промпт':<12} | {'Символов':>10} | {'Токенов':>10} | {'Символов/токен':>15}")
```

**Задача 5.2 — захардкоженный «XXX» вместо реального значения (–2 балла):**

```python
# Фактический код:
print(f'''[OK] Промпт содержит XXX токенов. Отправляю запрос...
[ОТВЕТ] {get_content}''')
```

Переменная `tokens` вычислена корректно, но не подставлена в строку. Это явная опечатка, которая нарушает ключевое требование задачи:

```python
# Должно быть:
print(f'[OK] Промпт содержит {tokens} токенов. Отправляю запрос...')
```

---

### Бонусное задание — 15/20 баллов

#### ✅ Что сделано правильно:

1. **Архитектура разбита на функции:**
```python
def check_num_tokens(prompt, max_tokens=300) -> tuple
def translate_review(prompt) -> str
def classification(prompt) -> str
```
Каждая функция выполняет ровно одну задачу — отличный подход.

2. **Все три отзыва обработаны корректно:**
```
=== ОБРАБОТКА ОТЗЫВА 1 ===
[OK] Проверка токенов пройдена (29 токенов)
Перевод: Эта книга изменила мою жизнь! ...
Настроение: {"sentiment":"positive","score":5}
```

3. **JSON-классификация с `response_format`** — правильно.

4. **Обработка ошибок через `try/except`** внутри цикла обработки отзывов.

5. **Проверка лимита токенов** перед каждым запросом.

#### ⚠️ Что нужно улучшить:

**Статистика генерируется через AI, а не программно (–3 балла):**

```python
# Фактически:
response = client.chat.completions.create(
    messages=[{'role': 'system', 'content': 'Проанализируй переданные данные и выведи отчет...'}],
    ...
)
```

Задание предполагало программное подведение итогов. Использование AI для подсчёта статистики добавляет непредсказуемость, стоимость и может дать неточные результаты. Правильный подход:

```python
sentiments = [json.loads(s)['sentiment'] for s in sentiment_results]
scores = [json.loads(s)['score'] for s in sentiment_results]

print(f"Обработано: {len(reviews)}/{len(reviews)}")
print(f"Positive: {sentiments.count('positive')} | Neutral: {sentiments.count('neutral')} | Negative: {sentiments.count('negative')}")
print(f"Средняя оценка: {sum(scores)/len(scores):.1f}/5")
```

**Неточный подсчёт токенов в итоге (–2 балла):**

В `for_analytics` сохраняется только количество токенов исходного текста отзыва (из `check_num_tokens`), а не реальные токены API-вызовов. Итоговый отчёт показывает «71 токен» — это сумма токенов исходных текстов отзывов, а не суммарные токены всех запросов к API (перевод + классификация × 3 = минимум 6 вызовов).

```python
# Правильный подсчёт:
total_api_tokens = 0

ru_response = client.chat.completions.create(...)
total_api_tokens += ru_response.usage.total_tokens

sentiment_response = client.chat.completions.create(...)
total_api_tokens += sentiment_response.usage.total_tokens
```

---

## Общие технические замечания

### ✅ Сильные стороны кода:

1. **Вспомогательная функция `clean_response()`:**
```python
def clean_response(text:str) -> str:
    pattern = r"<think>.*?</think>"
    cleaned_text = re.sub(pattern, "", text, flags=re.DOTALL).strip()
    return cleaned_text
```
Демонстрирует понимание архитектуры reasoning-моделей (DeepSeek-R1, Qwen-QwQ, Llama через Groq). ✅

2. **Единая инициализация клиента в начале ноутбука** — клиент создаётся один раз и переиспользуется.

3. **Использование `.env` и `os.getenv()`** — API-ключи защищены.

4. **Импорт всех библиотек в одной ячейке** — хорошая организация кода.

5. **Понимание `response.usage.total_tokens`** — использовано в задаче 2.2 для логирования.

### ⚠️ Технические рекомендации:

1. **Единообразный fallback для `api_key`:**

Паттерн `api_key=api_key or os.getenv('GROQ_TOKEN')` необходимо применять везде, где клиент создаётся с параметром `api_key`:

```python
def safe_request(prompt: str, api_key: str = None, role: str = 'user'):
    client_safe = OpenAI(
        api_key=api_key or os.getenv('GROQ_TOKEN'),  # ← всегда так
        base_url=os.getenv('BASE_URL_GROQ')
    )
```

2. **Применение `clean_response()` единообразно:**

В некоторых функциях (`safe_request`) `clean_response()` не применяется. Рекомендуется обернуть все получения ответа:
```python
return clean_response(response.choices[0].message.content)
```

3. **Избыточный импорт `tenacity` в задаче 4.2:**

В задаче 4.1 уже импортированы нужные компоненты. Задача 4.2 переопределяет `log_retry` и дублирует декоратор — можно было переиспользовать из 4.1.

---

## Итоговая таблица оценок

| Часть | Баллы | Максимум | Процент | Комментарий |
|-------|-------|----------|---------|-------------|
| **Часть 1:** Структурированный JSON | 16 | 20 | 80% | Неверный формат вывода в 1.1 (–2), сырой JSON вместо форматированного вывода в 1.2 (–2) |
| **Часть 2:** Обработка ошибок | 23 | 25 | 92% | Нет fallback на GROQ_TOKEN (–1), нет clean_response в safe_request (–1) |
| **Часть 3:** Батчинг | 16 | 20 | 80% | Задача 3.1 идеальна; в 3.2 отсутствует поле name, русские имена полей (–4) |
| **Часть 4:** Retry-логика | 18 | 20 | 90% | api_key баг в production_request (–2) |
| **Часть 5:** Подсчёт токенов | 12 | 15 | 80% | Формат таблицы (–1), захардкоженный «XXX» в 5.2 (–2) |
| **Бонус** | 15 | 20 | 75% | AI-генерация статистики вместо программной (–3), неточный подсчёт токенов (–2) |
| **ИТОГО (без бонуса)** | **85** | **100** | **85%** | Хорошая работа! |
| **ИТОГО (с бонусом)** | **100** | **120** | **83%** | |

---

## Чек-лист выполнения требований

- [x] **JSON-формат запрашивается корректно** — `response_format={'type': 'json_object'}` используется везде где нужно ✅
- [x] **Ответ парсится** — `json.loads()` применяется корректно ✅
- [x] **Все типы ошибок обрабатываются раздельно** — Auth, RateLimit, BadRequest, Exception ✅
- [x] **Логирование работает** — `logged_request()` выводит корректные логи ✅
- [x] **Оба варианта батчинга реализованы** — Вариант A и B с подсчётом токенов ✅
- [x] **Retry настроен корректно** — правильные типы ошибок, попытки, задержка ✅
- [x] **Retry объединён с обработкой ошибок** — `@retry` + `try/except` работают вместе ✅
- [ ] **Подсчёт токенов точный** — tiktoken работает, но «XXX» в выводе 5.2 ❌
- [ ] **Предварительная проверка работает корректно** — логика верная, но вывод с «XXX» ❌
- [ ] **Формат вывода соответствует требованиям** — в нескольких задачах формат отличается ❌

---

## Рекомендации для улучшения

### 1. Форматирование вывода (Части 1, 5)

Всегда проверять соответствие формата вывода требованиям задания перед финальной сдачей. Используйте f-strings с выравниванием:

```python
# Задача 1.1 — правильный формат:
print("=== IT-ПРОФЕССИИ ПО ЗАРПЛАТЕ ===")
for i, (_, row) in enumerate(sorted_df.iterrows(), 1):
    salary = f"${row['avg_salary_usd']:,}"
    print(f"{i}. {row['title']} — {salary}")

# Задача 5.1 — правильный формат таблицы:
print(f"{'Промпт':<12} | {'Символов':>10} | {'Токенов':>10} | {'Символов/токен':>15}")
print("-" * 55)
for name, prompt in prompts.items():
    num_chars = len(prompt)
    num_tokens = len(encoder.encode(prompt))
    ratio = num_chars / num_tokens
    print(f"{name:<12} | {num_chars:>10} | {num_tokens:>10} | {ratio:>14.2f}")
```

### 2. Явное указание полей JSON в промпте (Часть 3.2)

Всегда указывать в промпте точные английские имена полей с примером структуры:

```python
system_prompt = '''Тебе передают список смартфонов. Верни JSON со списком объектов.

Структура каждого объекта (строго соблюдай имена полей):
{
    "name": "полное название устройства",
    "brand": "бренд",
    "os": "операционная система",
    "price_usd": цена в долларах (целое число)
}

Пример:
{"name": "iPhone 15 Pro", "brand": "Apple", "os": "iOS", "price_usd": 1199}'''
```

### 3. Программная статистика вместо AI-генерации (Бонус)

```python
sentiments = []
scores = []
total_tokens = 0

for i, review in enumerate(reviews):
    # ... обработка ...
    sentiment_data = json.loads(sentiment)
    sentiments.append(sentiment_data['sentiment'])
    scores.append(sentiment_data['score'])
    total_tokens += check_tokens[1]  # или суммировать реальные API-токены

print("=== ИТОГ ===")
print(f"Обработано: {len(reviews)}/{len(reviews)}")
print(f"Positive: {sentiments.count('positive')} | "
      f"Neutral: {sentiments.count('neutral')} | "
      f"Negative: {sentiments.count('negative')}")
print(f"Средняя оценка: {sum(scores)/len(scores):.1f}/5")
print(f"Всего токенов использовано: {total_tokens}")
```

### 4. Единый клиент или фабрика клиентов

Вместо создания нового клиента в каждой функции — передавать глобальный `client` или инкапсулировать создание:

```python
def make_client(api_key: str = None) -> OpenAI:
    return OpenAI(
        api_key=api_key or os.getenv('GROQ_TOKEN'),
        base_url=os.getenv('BASE_URL_GROQ')
    )
```

---

## Сильные стороны решения

1. ✅ **Архитектура функций** — `clean_response()`, `safe_request()`, `logged_request()`, `production_request()` — грамотная инкапсуляция
2. ✅ **Работа с reasoning-моделями** — понимание тегов `<think>` и необходимость их фильтрации
3. ✅ **Задача 3.1 батчинг** — образцовое выполнение с наглядным сравнением (65% экономии)
4. ✅ **Retry-конфигурация** — все параметры задаче 4.1 заданы корректно
5. ✅ **Бонус: модульность пайплайна** — разбивка на три независимые функции
6. ✅ **Безопасность** — API-ключи через `.env`

---

## Заключение

Студент демонстрирует **хорошее понимание** принципов построения production-ready кода для работы с AI API:

- ✅ Умеет запрашивать и парсить структурированные JSON-ответы
- ✅ Правильно обрабатывает разные типы ошибок OpenAI API
- ✅ Понимает смысл батчинга и умеет его реализовать
- ✅ Корректно конфигурирует retry-логику с tenacity
- ✅ Работает с tiktoken для подсчёта токенов

**Основные области для улучшения:**
1. Точное соответствие формата вывода требованиям задания
2. Единообразный fallback для `api_key` во всех функциях
3. Корректное указание схемы JSON в промптах
4. Исправление опечатки «XXX» в задаче 5.2

**Итоговая оценка: 85/100 + 15 бонусных баллов — Хорошо!**

---

## Дополнительные материалы для изучения

Для дальнейшего роста рекомендую изучить:

1. **[[OpenAI_API/Structuring_End_to_End_Applications]]** — повторить раздел про Structured Outputs (Pydantic-схемы строже `json_object`)
2. **[[Python_for_AI/Tenacity_Library_for_Retry_Logic]]** — изучить `retry_if_result` для условного повтора по содержимому ответа
3. **[[Python_for_AI/Python_Decorators]]** — углубить понимание декораторов для логирования и мониторинга

**Следующее задание:** Переходите к изучению Function Calling — структурированные вызовы инструментов дают ещё более надёжное управление выводом модели!

---

*Успехов в дальнейшем изучении AI! 🚀*
