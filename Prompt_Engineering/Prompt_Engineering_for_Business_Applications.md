# Prompt Engineering для бизнес-приложений

## Введение

Данная заметка описывает практическое применение техник промпт-инжиниринга для решения реальных бизнес-задач. Рассматриваются четыре ключевые области: резюмирование и расширение текста, трансформация текста, анализ текста, а также генерация и объяснение кода.

**Для базовых техник** см. [[Prompt_Engineering/Prompt_Engineering_Best_Practices|Лучшие практики Prompt Engineering]].
**Для продвинутых стратегий** см. [[Prompt_Engineering/Advanced_Prompt_Engineering_Strategies|Продвинутые стратегии Prompt Engineering]].
**Для практического применения** см. [[Exercises/05_Business_Applications_Prompt_Engineering_Exercise|Практическое задание №5]].

---

## Инициализация API клиента

Во всех примерах используется следующая базовая конфигурация:

```python
from openai import OpenAI
from dotenv import load_dotenv
import os

load_dotenv()

client = OpenAI(
    api_key=os.getenv('GROQ_TOKEN'),  # или 'OPENAI_API_KEY'
    base_url=os.getenv('BASE_URL_GROQ')  # опционально для альтернативных провайдеров
)

def get_response(prompt: str, temperature: float = 1.0) -> str:
    """Базовая функция для получения ответа от модели"""
    response = client.chat.completions.create(
        model=os.getenv('GROQ_MODEL'),  # или 'gpt-4'
        messages=[{'role': 'user', 'content': prompt}],
        temperature=temperature
    )
    return response.choices[0].message.content
```

---

## 1. Резюмирование и расширение текста

### 1.1. Резюмирование текста (Summarization)

**Резюмирование** — процесс извлечения ключевой информации из длинного текста. В бизнесе используется для:
- **Финансы**: быстрое принятие решений на основе аналитических отчетов
- **Маркетинг**: извлечение insights из отзывов клиентов
- **HR**: обработка резюме и интервью

#### Базовое резюмирование

```python
review = """
Я купил умные часы SmartWatch Pro 5 месяц назад. Дизайн очень стильный,
экран AMOLED яркий и четкий. Батарея держит 2 дня при активном использовании.
Отслеживание активности работает точно. Однако синхронизация с телефоном
иногда прерывается, и приложение медленно загружается. В целом хороший продукт
за свою цену, но есть недочеты в программном обеспечении.
"""

prompt = f"""Резюмируй следующий отзыв о продукте:

{review}"""

print(get_response(prompt))
```

**Результат**:
> Умные часы SmartWatch Pro 5 имеют стильный дизайн и качественный AMOLED-экран. Батарея держит 2 дня, отслеживание активности точное. Минусы: проблемы с синхронизацией и медленное приложение.

#### Резюмирование с ограничениями

**Ограничение по длине**:
```python
prompt = f"""Резюмируй следующий отзыв в одно предложение (максимум 20 слов):

{review}"""
```

**Результат**:
> Качественные часы с хорошим экраном и батареей, но с проблемами синхронизации и приложения.

#### Структурированное резюмирование

**Маркированный список**:
```python
prompt = f"""Резюмируй ключевые моменты следующего отзыва в формате маркированного списка:

{review}"""
```

**Результат**:
- ✅ Стильный дизайн и яркий AMOLED-экран
- ✅ Батарея держит 2 дня при активном использовании
- ✅ Точное отслеживание активности
- ❌ Проблемы с синхронизацией с телефоном
- ❌ Медленная загрузка приложения

#### Фокусное резюмирование

**Резюмирование с акцентом на конкретные аспекты**:

```python
# Фокус на пользовательском опыте
prompt = f"""Резюмируй следующий отзыв, сфокусировавшись на пользовательском опыте:

{review}"""

# Фокус на ключевых характеристиках
prompt = f"""Извлеки из следующего отзыва только ключевые характеристики продукта
(экран, батарея, функции) в формате маркированного списка:

{review}"""
```

**Результат (фокус на характеристиках)**:
- Экран: AMOLED, яркий и четкий
- Батарея: держит 2 дня при активном использовании
- Функции: точное отслеживание активности

---

### 1.2. Расширение текста (Text Extension)

**Расширение текста** — обратный процесс резюмирования. Используется для:
- Генерации развернутых email-сообщений из ключевых пунктов
- Создания отчетов на основе кратких данных
- Написания маркетинговых текстов из характеристик продукта

#### Базовое расширение

```python
key_points = """
- Новый продукт: SmartHome Hub 2.0
- Управление всеми умными устройствами
- Голосовое управление через Alexa и Google Assistant
- Цена: $199
"""

prompt = f"""Создай развернутое описание продукта для маркетинговой рассылки
на основе следующих ключевых пунктов:

{key_points}

Требования:
- Длина: 150-200 слов
- Тон: профессиональный, но дружелюбный
- Целевая аудитория: технически подкованные пользователи
- Включи призыв к действию в конце"""

print(get_response(prompt))
```

**Результат**:
> Представляем SmartHome Hub 2.0 — новое поколение управления вашим умным домом. Это централизованное решение позволяет вам контролировать все ваши умные устройства из одного места, обеспечивая беспрецедентное удобство и эффективность.
>
> Благодаря интеграции с голосовыми ассистентами Alexa и Google Assistant, вы можете управлять освещением, термостатом, камерами безопасности и другими устройствами простыми голосовыми командами. Больше не нужно переключаться между множеством приложений — SmartHome Hub 2.0 объединяет всё в единую экосистему.
>
> По цене всего $199, это доступное решение для тех, кто ценит инновации и комфорт. Откройте для себя будущее умного дома уже сегодня!
>
> **Закажите SmartHome Hub 2.0 прямо сейчас и получите бесплатную доставку!**

#### Расширение с изменением тона

**Формальный email**:
```python
notes = "Встреча перенесена на четверг, 15:00. Нужно обсудить Q4 бюджет."

prompt = f"""Преобразуй следующие заметки в профессиональное email-сообщение
для отправки команде:

{notes}

Требования:
- Формальный тон
- Структура: приветствие, сообщение, призыв к действию
- Длина: 3-4 предложения"""
```

**Результат**:
> Уважаемая команда,
>
> Хочу сообщить вам, что наша встреча была перенесена на четверг в 15:00. На повестке дня будет обсуждение бюджета на четвертый квартал.
>
> Пожалуйста, подготовьте свои предложения и вопросы заранее.
>
> С уважением,
> [Ваше имя]

---

## 2. Трансформация текста

### 2.1. Перевод (Translation)

#### Базовый перевод

```python
text = "Искусственный интеллект меняет способ ведения бизнеса."

prompt = f"""Переведи следующий текст с русского на английский:

{text}"""

print(get_response(prompt))
# Вывод: Artificial intelligence is changing the way business is conducted.
```

#### Автоопределение языка

```python
text = "Machine learning is a subset of artificial intelligence."

prompt = f"""Определи язык следующего текста и переведи его на русский:

{text}"""

# Вывод: Язык: английский
# Перевод: Машинное обучение является подмножеством искусственного интеллекта.
```

#### Множественный перевод

```python
text = "Добро пожаловать в наш сервис!"

prompt = f"""Переведи следующий текст на три языка: английский, французский и испанский.
Формат вывода:
- Английский: [перевод]
- Французский: [перевод]
- Испанский: [перевод]

Текст: {text}"""

# Вывод:
# - Английский: Welcome to our service!
# - Французский: Bienvenue dans notre service !
# - Испанский: ¡Bienvenido a nuestro servicio!
```

**⚠️ Важно**: Для клиентоориентированного контента рекомендуется проверять качество перевода через человека или специализированные переводческие сервисы.

---

### 2.2. Изменение тона (Tone Adjustment)

#### Из неформального в формальный

```python
casual_text = "Привет! Слушай, насчет того проекта — давай встретимся завтра?"

prompt = f"""Преобразуй следующий текст из неформального в формальный деловой стиль:

{casual_text}"""

# Вывод: Добрый день! Предлагаю обсудить детали проекта на встрече завтра.
# Подтвердите, пожалуйста, вашу доступность.
```

#### Из технического в простой

```python
technical_text = """
API использует RESTful архитектуру с JSON payload.
Аутентификация осуществляется через OAuth 2.0 токены.
"""

prompt = f"""Перепиши следующий технический текст простым языком для нетехнической аудитории:

{technical_text}"""

# Вывод: Наша система использует стандартный веб-интерфейс для обмена данными.
# Для безопасности вход осуществляется через специальные временные коды доступа.
```

---

### 2.3. Улучшение текста (Writing Improvement)

#### Исправление грамматики и орфографии

```python
text_with_errors = """
Наша компания предостовляет услуги в области машиного обучения.
Мы помогаим бизнесу оптимизировать процесы и увеличить прибыль.
"""

prompt = f"""Исправь все орфографические и грамматические ошибки в следующем тексте:

{text_with_errors}"""

# Вывод: Наша компания предоставляет услуги в области машинного обучения.
# Мы помогаем бизнесу оптимизировать процессы и увеличить прибыль.
```

#### Улучшение читаемости

```python
messy_text = """
Мы предоставляем различные услуги включая консалтинг и разработку
также мы помогаем с внедрением и обучением персонала а еще у нас
есть техподдержка 24/7 которая всегда готова помочь
"""

prompt = f"""Улучши читаемость следующего текста:
- Раздели на предложения
- Добавь пунктуацию
- Структурируй информацию логично

{messy_text}"""

# Вывод: Мы предоставляем комплексные услуги: консалтинг, разработку,
# внедрение и обучение персонала. Наша техническая поддержка работает
# круглосуточно и всегда готова помочь вам.
```

#### Многоэтапная трансформация

**Пример: Превращение неформальной заметки в профессиональное письмо**:

```python
informal_note = """
Привет команда
вчера мы обсуждали новый проект типа надо бы начать в следующем месяце
кто будет заниматься дизайном? также нужно подумать о бюджете
"""

prompt = """Выполни следующие трансформации текста:
1. Исправь грамматику и пунктуацию
2. Преобразуй в формальный деловой стиль
3. Структурируй в формате email с приветствием и призывом к действию
4. Добавь ясность и конкретность

Текст:
Привет команда
вчера мы обсуждали новый проект типа надо бы начать в следующем месяце
кто будет заниматься дизайном? также нужно подумать о бюджете
"""

# Вывод:
# Добрый день, команда!
#
# На вчерашней встрече мы обсудили запуск нового проекта, намеченный на следующий месяц.
# Для дальнейшего планирования необходимо решить два ключевых вопроса:
#
# 1. Назначение ответственного за дизайн проекта
# 2. Определение бюджетных рамок
#
# Прошу вас подготовить предложения по этим вопросам к пятнице.
#
# С уважением,
# [Ваше имя]
```

---

## 3. Анализ текста

### 3.1. Классификация (Classification)

#### Анализ настроений (Sentiment Analysis)

```python
customer_feedback = "Продукт работает нормально, но доставка была очень медленной."

prompt = f"""Определи общий тон (sentiment) следующего отзыва клиента.
Классификация: Позитивный, Негативный или Нейтральный.

Отзыв: {customer_feedback}

Ответь только одним словом: Позитивный, Негативный или Нейтральный."""

# Вывод: Нейтральный
```

#### Определение эмоций

```python
text = "Я в восторге от нового обновления! Всё работает просто потрясающе!"

prompt = f"""Определи до 3 доминирующих эмоций в следующем тексте.
Возможные эмоции: радость, грусть, гнев, страх, удивление, разочарование, восторг.

Текст: {text}

Формат ответа: список эмоций через запятую."""

# Вывод: восторг, радость, удивление
```

#### Многоклассовая классификация

**⚠️ Важно**: При классификации **всегда явно определяйте все возможные категории** в промпте для повышения точности.

```python
support_ticket = """
Тема: Проблема со входом
Я не могу войти в свой аккаунт уже три дня. Пробовал сбросить пароль,
но письмо не приходит. Очень срочно, так как у меня важная презентация завтра!
"""

prompt = f"""Классифицируй следующий тикет в службу поддержки по двум параметрам:

Категория проблемы: Технические проблемы, Вопросы об оплате, Общие вопросы, Жалоба
Приоритет: Низкий, Средний, Высокий, Критический

Тикет:
{support_ticket}

Формат ответа:
- Категория: [категория]
- Приоритет: [приоритет]"""

# Вывод:
# - Категория: Технические проблемы
# - Приоритет: Высокий
```

---

### 3.2. Извлечение сущностей (Entity Extraction)

#### Базовое извлечение

```python
product_description = """
Представляем ноутбук TechPro X15 с 15.6-дюймовым OLED-дисплеем,
процессором Intel Core i7-13700H, 32 ГБ оперативной памяти и SSD на 1 ТБ.
Камера на 1080p обеспечивает четкую видеосвязь. Цена: $1,499.
"""

prompt = f"""Извлеки из следующего описания продукта следующие параметры:
- Название модели
- Размер экрана
- Тип экрана
- Процессор
- Оперативная память
- Объем SSD
- Разрешение камеры
- Цена

Описание:
{product_description}

Формат ответа: маркированный список"""

# Вывод:
# - Название модели: TechPro X15
# - Размер экрана: 15.6 дюймов
# - Тип экрана: OLED
# - Процессор: Intel Core i7-13700H
# - Оперативная память: 32 ГБ
# - Объем SSD: 1 ТБ
# - Разрешение камеры: 1080p
# - Цена: $1,499
```

#### Извлечение с few-shot learning

**Применение**: Обработка тикетов службы поддержки для извлечения структурированных данных.

```python
# Примеры для few-shot learning
example_1 = """
Вход: Здравствуйте! Меня зовут Иван Петров, мой телефон +7-905-123-4567.
Я бронировал номер на 15 марта, код брони: ABC123. Хочу перенести на 20 марта.

Выход:
- Имя клиента: Иван Петров
- Телефон: +7-905-123-4567
- Код брони: ABC123
- Текущая дата: 15 марта
- Новая дата: 20 марта
"""

example_2 = """
Вход: Добрый день, я Мария Смирнова, +7-916-999-8888.
Нужно отменить бронь XYZ789 на 10 апреля.

Выход:
- Имя клиента: Мария Смирнова
- Телефон: +7-916-999-8888
- Код брони: XYZ789
- Дата: 10 апреля
- Действие: отменить
"""

new_ticket = """
Привет, это Алексей Сидоров, контакт +7-903-555-7777.
Моя бронь DEF456 на 25 марта, хочу добавить еще одну ночь.
"""

prompt = f"""Извлеки структурированную информацию из тикета клиента.

{example_1}

{example_2}

Вход: {new_ticket}

Выход:"""

# Вывод:
# - Имя клиента: Алексей Сидоров
# - Телефон: +7-903-555-7777
# - Код брони: DEF456
# - Дата: 25 марта
# - Действие: продлить на одну ночь
```

#### Извлечение с JSON-форматом

```python
email_text = """
От: john.doe@techcorp.com
Тема: Предложение о партнерстве
Дата: 15 января 2024

Добрый день!

Я Джон Доу, руководитель отдела развития бизнеса в TechCorp Inc.
Хотел бы обсудить возможности партнерства. Можем встретиться на следующей неделе?
"""

prompt = f"""Извлеки ключевую информацию из следующего email и верни результат в формате JSON:

{email_text}

Извлеки:
- sender_email
- sender_name
- sender_position
- company
- subject
- date
- intent (намерение отправителя)

Формат вывода: JSON"""

# Вывод:
# {
#   "sender_email": "john.doe@techcorp.com",
#   "sender_name": "Джон Доу",
#   "sender_position": "Руководитель отдела развития бизнеса",
#   "company": "TechCorp Inc",
#   "subject": "Предложение о партнерстве",
#   "date": "15 января 2024",
#   "intent": "Обсудить возможности партнерства"
# }
```

---

## 4. Генерация и объяснение кода

### 4.1. Генерация кода (Code Generation)

#### Базовая генерация

```python
problem = """
Создай Python-скрипт, который вычисляет средний квартальный объем продаж.
Входные данные: список продаж по месяцам за год (12 чисел).
Выходные данные: список из 4 чисел (средний объем продаж для каждого квартала).
"""

prompt = f"""Сгенерируй Python код для решения следующей задачи:

{problem}

Требования:
- Используй функцию (не просто скрипт)
- Добавь docstring с описанием
- Проверь корректность входных данных"""

# Вывод:
# def calculate_quarterly_avg(monthly_sales):
#     """
#     Вычисляет средний квартальный объем продаж.
#
#     Args:
#         monthly_sales (list): Список продаж по месяцам (12 чисел)
#
#     Returns:
#         list: Список из 4 средних значений для каждого квартала
#     """
#     if len(monthly_sales) != 12:
#         raise ValueError("Требуется 12 значений (по одному на каждый месяц)")
#
#     quarterly_avg = []
#     for i in range(0, 12, 3):
#         quarter = monthly_sales[i:i+3]
#         avg = sum(quarter) / len(quarter)
#         quarterly_avg.append(avg)
#
#     return quarterly_avg
```

#### Генерация с примерами входа/выхода

**💡 Подсказка**: Предоставление примеров **значительно улучшает качество** генерируемого кода.

```python
prompt = """Создай Python функцию для вычисления среднего квартального объема продаж.

Вход: список из 12 чисел (продажи по месяцам)
Выход: список из 4 чисел (средние продажи по кварталам)

Пример:
Вход: [100, 150, 200, 180, 220, 250, 300, 280, 320, 350, 400, 420]
Выход: [150.0, 216.67, 300.0, 390.0]

Требования:
- Функция с docstring
- Проверка корректности входных данных
- Обработка ошибок"""

# Код будет более точным и надежным благодаря примерам
```

---

### 4.2. Модификация кода (Code Modification)

#### Преобразование скрипта в функцию

```python
script = """
sales = [100, 150, 200, 180, 220, 250, 300, 280, 320, 350, 400, 420]
q1 = (sales[0] + sales[1] + sales[2]) / 3
q2 = (sales[3] + sales[4] + sales[5]) / 3
q3 = (sales[6] + sales[7] + sales[8]) / 3
q4 = (sales[9] + sales[10] + sales[11]) / 3
print([q1, q2, q3, q4])
"""

prompt = f"""Преобразуй следующий Python скрипт в переиспользуемую функцию:

```python
{script}
```

Требования:
- Создай функцию с параметрами
- Добавь docstring
- Верни результат вместо print"""
```

#### Добавление интерактивности

```python
function = """
def calculate_quarterly_avg(monthly_sales):
    quarterly_avg = []
    for i in range(0, 12, 3):
        quarter = monthly_sales[i:i+3]
        avg = sum(quarter) / len(quarter)
        quarterly_avg.append(avg)
    return quarterly_avg
"""

prompt = f"""Модифицируй следующую функцию, добавив интерактивный ввод данных от пользователя:

```python
{function}
```

Требования:
1. Запроси у пользователя ввод 12 чисел (продажи по месяцам)
2. Вызови функцию с введенными данными
3. Выведи результат в удобочитаемом формате"""
```

#### Множественные модификации

```python
prompt = f"""Модифицируй следующую функцию:

```python
{function}
```

Выполни следующие изменения:
1. Добавь интерактивный ввод данных от пользователя через input()
2. Добавь валидацию: проверь, что введены корректные числа
3. Обработай ошибки (try-except) для некорректного ввода
4. Выведи результат в формате "Q1: [значение], Q2: [значение], ..."
"""

# Модель может выполнить несколько модификаций за один проход
```

---

### 4.3. Объяснение кода (Code Explanation)

#### Краткое объяснение

```python
code = """
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)
"""

prompt = f"""Объясни следующий код в одном предложении:

```python
{code}
```"""

# Вывод: Рекурсивная функция для вычисления n-го числа Фибоначчи
# путем сложения двух предыдущих чисел последовательности.
```

#### Пошаговое объяснение (Chain-of-Thought)

```python
code = """
def quicksort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quicksort(left) + middle + quicksort(right)
"""

prompt = f"""Объясни следующий код шаг за шагом, как работает алгоритм:

```python
{code}
```

Для каждой строки опиши:
1. Что делает эта строка
2. Зачем она нужна
3. Какие данные обрабатывает"""

# Вывод: подробное пошаговое объяснение алгоритма быстрой сортировки
```

#### Объяснение с визуализацией работы

```python
prompt = f"""Объясни следующий код и покажи пример его работы на конкретных данных:

```python
{code}
```

Пример: arr = [3, 6, 8, 10, 1, 2, 1]

Покажи:
1. Краткое описание алгоритма
2. Пошаговое выполнение на примере данных
3. Временную сложность алгоритма"""

# Вывод: полное объяснение с визуализацией каждого рекурсивного вызова
```

---

## Практические паттерны

### Паттерн 1: Итеративное уточнение промпта

Для сложных задач используйте итеративный подход:

```python
# Итерация 1: Базовый запрос
prompt_v1 = "Создай email о переносе встречи"

# Итерация 2: Добавление контекста
prompt_v2 = "Создай формальный email для отправки клиенту о переносе встречи с пятницы на понедельник"

# Итерация 3: Уточнение деталей
prompt_v3 = """Создай формальный email для отправки клиенту о переносе встречи.

Контекст:
- Встреча была назначена на пятницу, 10:00
- Переносится на понедельник, 14:00
- Причина: непредвиденные обстоятельства

Требования:
- Профессиональный тон
- Извинения за неудобства
- Подтверждение новой даты
- Запрос подтверждения"""
```

### Паттерн 2: Комбинирование задач

```python
# Комбинация: Анализ + Резюмирование + Рекомендации
long_customer_review = "..." # длинный отзыв

prompt = f"""Выполни следующие задачи с отзывом клиента:

1. Определи общий sentiment (Позитивный/Негативный/Нейтральный)
2. Извлеки упомянутые проблемы (если есть)
3. Резюмируй отзыв в 2-3 предложениях
4. Предложи 2-3 конкретных действия для службы поддержки

Отзыв:
{long_customer_review}

Формат вывода:
- Sentiment: [...]
- Проблемы: [...]
- Резюме: [...]
- Рекомендации: [...]"""
```

### Паттерн 3: Шаблоны для повторяющихся задач

```python
def create_business_prompt(
    task_type: str,
    input_data: str,
    output_format: str = "текст",
    constraints: list = None,
    tone: str = "профессиональный"
) -> str:
    """
    Универсальный генератор промптов для бизнес-задач.

    Args:
        task_type: Тип задачи (резюмирование, перевод, классификация и т.д.)
        input_data: Входные данные
        output_format: Формат вывода (текст, JSON, список и т.д.)
        constraints: Список ограничений
        tone: Требуемый тон (профессиональный, формальный, дружелюбный)

    Returns:
        Сформированный промпт
    """
    prompt_parts = [
        f"Задача: {task_type}",
        f"Тон: {tone}",
        f"Формат вывода: {output_format}"
    ]

    if constraints:
        prompt_parts.append("\nОграничения:")
        prompt_parts.extend([f"- {c}" for c in constraints])

    prompt_parts.append(f"\nВходные данные:\n{input_data}")

    return "\n".join(prompt_parts)


# Использование
prompt = create_business_prompt(
    task_type="Резюмирование отзыва клиента",
    input_data=review,
    output_format="маркированный список (ключевые моменты)",
    constraints=["Не более 5 пунктов", "Выдели плюсы и минусы отдельно"],
    tone="нейтральный"
)
```

---

## Рекомендации по применению

### 1. Управление temperature

```python
# Резюмирование, классификация, извлечение данных
# Требуется точность и стабильность
get_response(prompt, temperature=0.0)

# Генерация текста, расширение
# Требуется баланс креативности и точности
get_response(prompt, temperature=0.7)

# Творческие задачи, генерация идей
# Требуется максимальная креативность
get_response(prompt, temperature=1.2)
```

### 2. Использование разделителей

Всегда четко выделяйте входные данные:

```python
# ✅ Хорошо
prompt = f"""Задача: Резюмируй текст

Текст:
```
{text}
```
"""

# ❌ Плохо
prompt = f"Резюмируй текст {text}"
```

### 3. Тестирование на граничных случаях

```python
test_cases = [
    "",  # Пустой ввод
    "a" * 10000,  # Очень длинный текст
    "12345",  # Числовой ввод вместо текста
    "混合 mixed языков languages",  # Смешанные языки
]

for test_case in test_cases:
    result = get_response(create_prompt(test_case))
    print(f"Input: {test_case[:50]}... | Output: {result}")
```

### 4. Обработка ошибок

```python
def safe_llm_call(prompt: str, max_retries: int = 3) -> str:
    """Безопасный вызов LLM с retry-логикой"""
    for attempt in range(max_retries):
        try:
            response = get_response(prompt)
            if response and len(response) > 0:
                return response
        except Exception as e:
            if attempt == max_retries - 1:
                raise
            time.sleep(2 ** attempt)  # Exponential backoff

    return "Не удалось получить ответ"
```

---

## Бизнес-метрики эффективности

При внедрении LLM-решений в бизнес отслеживайте следующие метрики:

### Экономия времени

```python
# Пример: Обработка email
manual_time_per_email = 5  # минут
automated_time_per_email = 0.5  # минут
emails_per_day = 100

time_saved_per_day = (manual_time_per_email - automated_time_per_email) * emails_per_day
print(f"Экономия времени: {time_saved_per_day} минут в день ({time_saved_per_day/60:.1f} часов)")
```

### Качество результатов

- **Accuracy** (точность): для классификации и извлечения данных
- **BLEU/ROUGE score**: для резюмирования и перевода
- **User satisfaction**: опросы пользователей
- **Error rate**: процент ошибок, требующих ручного вмешательства

### Стоимость

```python
# Пример расчета стоимости API-вызовов
def calculate_cost(prompt: str, response: str, model: str = "gpt-4"):
    # Цены за 1K токенов (пример)
    prices = {
        "gpt-4": {"input": 0.03, "output": 0.06},
        "gpt-3.5-turbo": {"input": 0.0015, "output": 0.002}
    }

    # Приблизительная оценка: 1 токен ≈ 0.75 слова (для английского)
    input_tokens = len(prompt.split()) / 0.75
    output_tokens = len(response.split()) / 0.75

    cost = (
        (input_tokens / 1000) * prices[model]["input"] +
        (output_tokens / 1000) * prices[model]["output"]
    )

    return cost

# Расчет стоимости за период
daily_requests = 1000
avg_cost_per_request = 0.05  # долларов
monthly_cost = daily_requests * avg_cost_per_request * 30
print(f"Ожидаемая стоимость: ${monthly_cost} в месяц")
```

---

## Связанные материалы

- [[Prompt_Engineering/Prompt_Engineering_Best_Practices|Лучшие практики Prompt Engineering]] - базовые техники и принципы построения эффективных промптов
- [[Prompt_Engineering/Advanced_Prompt_Engineering_Strategies|Продвинутые стратегии Prompt Engineering]] - Zero/One/Few-Shot, Chain-of-Thought, Multi-Step, Self-Consistency
- [[OpenAI_API/Working_with_OpenAI_API_in_Python|Работа с OpenAI API в Python]] - практическая реализация API-вызовов
- [[OpenAI_API/Introduction_to_OpenAI_API|Введение в OpenAI API]] - основы работы с API
- [[OpenAI_API/Chat_Roles_and_Multi_Turn_Conversations|Роли в чатах и многоэтапные диалоги]] - использование system, user и assistant ролей для управления поведением модели
- [[Python_for_AI/Python_Decorators|Python Декораторы]] - использование декораторов для обработки ошибок и мониторинга
- [[Python_for_AI/Tenacity_Library_for_Retry_Logic|Библиотека Tenacity для retry-логики]] - надежные API-вызовы с автоматическими повторами

---

## Дополнительные ресурсы

- [OpenAI Use Cases](https://platform.openai.com/docs/examples) - примеры практического применения
- [Prompt Engineering for Business](https://www.coursera.org/learn/prompt-engineering) - курс по промпт-инжинирингу для бизнеса
- [LangChain Documentation](https://python.langchain.com/docs/get_started/introduction) - библиотека для построения LLM-приложений
- [OpenAI Cookbook](https://github.com/openai/openai-cookbook) - готовые рецепты для типовых задач

---

**Ключевой вывод**: Prompt engineering для бизнес-приложений охватывает четыре ключевые области (резюмирование/расширение, трансформация, анализ, генерация кода), каждая из которых решает конкретные бизнес-задачи. Успешное применение требует правильного выбора temperature, четкой структуры промптов, итеративного уточнения и постоянного мониторинга качества и стоимости.
