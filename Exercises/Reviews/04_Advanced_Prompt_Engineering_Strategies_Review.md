# Ревью практического задания №4: Продвинутые стратегии Prompt Engineering (v2)

**Студент**: [Имя студента]
**Дата проверки**: 2026-02-06 (повторная проверка)
**Проверяющий**: Claude Code
**Задание**: [[Exercises/04_Advanced_Prompt_Engineering_Strategies_Exercise|Практическое задание №4]]
**Решение**: [[Exercises/Solves/04_Advanced_Prompt_Engineering_Strategies_Solve|Решение студента]]

---

## Общая оценка: 76/100 баллов (+4 к первой версии)

### Что было исправлено

✅ **Исправлены ошибки в категориях Few-Shot примеров**:
- Задача 1.1 One-shot: `"Покупка продуктов -> ПРОДУКТЫ"` (было: ПИТАНИЕ) ✅
- Задача 1.1 Few-shot: `"Покупка продуктов 1000 рублей -> ПРОДУКТЫ"` (было: ПИТАНИЕ) ✅
- Задача 1.2 Диалог: `{'role':'assistant','content':'ПРОДУКТЫ'}` (было: 'ПИТАНИЕ') ✅

**Баллы за исправление**: +4 балла

### Что все еще требует исправления

❌ **Критическая проблема: отсутствие аналитических разделов**

Основная проблема из первого ревью **НЕ исправлена** - по-прежнему отсутствуют разделы анализа и сравнения результатов. Это главное требование задания!

---

## Детальный анализ по частям

### ✅ Часть 1: Shot Prompting (16/20 баллов) → +4 балла

#### Задача 1.1: Сравнение Zero-shot, One-shot и Few-shot (8/10 баллов)

**Что исправлено:**
- ✅ Категории в примерах исправлены на правильные (+2 балла)
- ✅ Все транзакции классифицированы правильно

**Проблемы (не исправлены):**
- ❌ **По-прежнему отсутствует обязательный раздел анализа (−2 балла)**

Задание требует:
```
=== АНАЛИЗ ===
Совпадений Zero-shot vs Few-shot: X из 6
Различия: ...
Вывод: [Какой подход лучше и почему]
```

**Как исправить:**
```python
# После выполнения всех трех промптов добавьте:
zero_result = get_response(zero_shot_prompt)
one_result = get_response(one_shot_prompt)
few_result = get_response(few_shot_prompt)

print('==> ZERO-SHOT <==')
print(zero_result)
print('==> ONE-SHOT <==')
print(one_result)
print('==> FEW-SHOT <==')
print(few_result)

# КРИТИЧЕСКИ ВАЖНО: добавьте анализ
print('\n' + '='*60)
print('СРАВНИТЕЛЬНЫЙ АНАЛИЗ')
print('='*60)

# Извлечение категорий (упрощенно)
def extract_categories(text):
    categories = []
    for line in text.split('\n'):
        for cat in ['РАЗВЛЕЧЕНИЯ', 'ПРОДУКТЫ', 'ТРАНСПОРТ', 'ЖКХ', 'ОБРАЗОВАНИЕ', 'ПИТАНИЕ']:
            if cat in line:
                categories.append(cat)
                break
    return categories

zero_cats = extract_categories(zero_result)
one_cats = extract_categories(one_result)
few_cats = extract_categories(few_result)

print(f"\nРезультаты:")
for i, trans in enumerate(transactions):
    print(f"{i+1}. {trans[:40]:40} | Zero: {zero_cats[i]:12} | One: {one_cats[i]:12} | Few: {few_cats[i]:12}")

matches = sum(1 for z, f in zip(zero_cats, few_cats) if z == f)
print(f"\n✅ Совпадений Zero-shot vs Few-shot: {matches} из {len(transactions)}")

print("\n📊 ВЫВОДЫ:")
print("- Все подходы справились с классификацией")
print("- Zero-shot эффективен благодаря четким описаниям категорий")
print("- Few-shot с правильными примерами наиболее надежен")
print("- One-shot достаточен для простых задач классификации")
```

---

#### Задача 1.2: Few-shot через диалог (8/10 баллов)

**Что исправлено:**
- ✅ Категория в примере исправлена на правильную (+2 балла)
- ✅ Структура user-assistant корректна

**Проблемы (не исправлены):**
- ❌ **Отсутствует сравнение с текстовым few-shot (−2 балла)**

Задание требует:
```
=== СРАВНЕНИЕ ПОДХОДОВ ===
Текстовый few-shot: [результат]
Диалоговый few-shot: [результат]
Вывод: [Есть ли разница в качестве?]
```

**Как исправить:**
```python
# После получения результата диалогового подхода
dialog_result = response.choices[0].message.content
text_result = get_response(few_shot_prompt)  # Из задачи 1.1

print('\n' + '='*60)
print('СРАВНЕНИЕ ПОДХОДОВ')
print('='*60)

print(f"\nТекстовый few-shot результат: {text_result}")
print(f"\nДиалоговый few-shot результат: {dialog_result}")

print("\n📊 АНАЛИЗ РАЗЛИЧИЙ:")
print("✅ Оба подхода дали правильный результат")
print("✅ Диалоговый подход:")
print("   - Более структурирован (роли явно разделены)")
print("   - Модель лучше понимает паттерн через примеры сообщений")
print("   - Предпочтителен при работе через API")
print("✅ Текстовый few-shot:")
print("   - Проще для быстрого прототипирования")
print("   - Компактнее записывается")
```

---

### ✅ Часть 2: Multi-Step Prompting (7/10 баллов) → без изменений

**Проблемы (не исправлены):**
- ❌ Отсутствует сравнение полноты ответов (−2 балла)
- ⚠️ Результаты частично не видны (−1 балл)

**Как исправить:**
```python
simple_result = get_response(simple_prompt)
multi_result = get_response(multi_step_prompt)

print('==> SIMPLE PROMPT <==')
print(simple_result[:1000])  # Ограничим вывод

print('\n==> MULTI-STEP PROMPT <==')
print(multi_result[:1500])  # Ограничим вывод

# КРИТИЧЕСКИ ВАЖНО: добавьте сравнение
print('\n' + '='*60)
print('СРАВНЕНИЕ ПОДХОДОВ')
print('='*60)

print(f"\n📏 Длина ответов:")
print(f"   Simple: {len(simple_result)} символов")
print(f"   Multi-step: {len(multi_result)} символов")

print(f"\n📊 Структурированность:")
steps = ['Шаг 1', 'Шаг 2', 'Шаг 3', 'Шаг 4', 'Шаг 5', 'Шаг 6']
for step in steps:
    present = '✓' if step in multi_result else '✗'
    print(f"   {present} {step} присутствует в multi-step")

print(f"\n✅ ВЫВОД:")
print("   Multi-step дает более полный и структурированный анализ")
print("   Все 6 требуемых аспектов покрыты")
```

---

### ✅ Часть 3: Chain-of-Thought (15/20 баллов) → без изменений

#### Задача 3.1 (7/10 баллов) и 3.2 (8/10 баллов)

**Проблемы (не исправлены):**
- ❌ Отсутствует ручная проверка правильности (−3 балла)
- ❌ Нет анализа преимуществ CoT (−2 балла)

**Как исправить:**
```python
# Для задачи 3.1
print('\n' + '='*60)
print('РУЧНАЯ ПРОВЕРКА ПРАВИЛЬНОСТИ')
print('='*60)

print("\nЗадача: 50 сотрудников, 60% MacBook, 40% Windows")
print("Из MacBook: 40% разработчики, из Windows: 25% разработчики")

print("\n✓ Шаг 1: MacBook = 50 * 0.6 = 30")
print("✓ Шаг 2: Windows = 50 * 0.4 = 20")
print("✓ Шаг 3: Разработчики MacBook = 30 * 0.4 = 12")
print("✓ Шаг 4: Разработчики Windows = 20 * 0.25 = 5")
print("✓ Шаг 5: Всего = 12 + 5 = 17")

print("\n✅ Модель справилась отлично! Все рассуждения верны.")

print("\n📊 ПРЕИМУЩЕСТВА CHAIN-OF-THOUGHT:")
print("+ Видны промежуточные вычисления")
print("+ Можно проверить каждый шаг")
print("+ Легче найти ошибку, если ответ неправильный")
print("+ Повышает доверие к результату")

# Для задачи 3.2
print('\n' + '='*60)
print('ПРОВЕРКА ПРАВИЛЬНОСТИ (Задача 3.2)')
print('='*60)

print("\n✓ Любители кофе: 80 * 0.45 = 36")
print("✓ Кофе с сахаром: 36 * 0.6 = 21.6")
print("✓ Любители чая: 80 * 0.35 = 28")
print("✓ Чай с сахаром: 28 * 0.4 = 11.2")
print("✓ Итого: 21.6 + 11.2 = 32.8 (или 33 человека)")

print("\n✅ Модель справилась отлично! Few-shot + CoT работает идеально.")
```

---

### ✅ Часть 4: Self-Consistency (7/10 баллов) → без изменений

**Проблемы (не исправлены):**
- ❌ Не извлечены и не сохранены ответы для анализа (−2 балла)
- ⚠️ Результаты частично не видны (−1 балл)

**Как исправить:**
```python
import re

question = '''...'''
prompt = f"""..."""

results = []

for i in range(3):
    print(f'==> ПОПЫТКА {i+1} <==')
    response = get_response(prompt)
    print(response[:500])  # Ограничим вывод

    # КРИТИЧЕСКИ ВАЖНО: извлекайте ответ
    match = re.search(r'Ответ.*?(\d+)', response)
    if match:
        answer = int(match.group(1))
        results.append(answer)
        print(f"\n→ Извлеченный ответ: {answer}")

# Анализ консенсуса
from collections import Counter
print('\n' + '='*60)
print('АНАЛИЗ КОНСЕНСУСА')
print('='*60)

consensus = Counter(results).most_common(1)[0]
print(f"\n📊 Результаты всех попыток: {results}")
print(f"✅ Консенсусный ответ: {consensus[0]} (встретился {consensus[1]} раз из {len(results)})")

if len(set(results)) == 1:
    print("✅ Стабильность: 100% (все ответы одинаковые)")
else:
    print(f"⚠️ Стабильность: варьируется между {min(results)} и {max(results)}")
    print("   Это связано с неоднозначностью задачи (дробные числа)")
```

---

### ✅ Часть 5: Iterative Prompting (15/20 баллов) → без изменений

#### Задача 5.1 (7/10 баллов) и 5.2 (8/10 баллов)

**Проблемы (не исправлены):**
- ❌ Отсутствует структурированный анализ улучшений (−3 балла)
- ⚠️ Результаты частично не видны (−2 балла)

**Как исправить:**
```python
# После всех итераций в задаче 5.1
print('\n' + '='*60)
print('ИТОГОВЫЙ АНАЛИЗ ПРОГРЕССИИ')
print('='*60)

improvements = [
    "Итерация 0: Базовый анализ - одно предложение, неструктурирован",
    "Итерация 1: + Структура (язык, описание, сложность)",
    "Итерация 2: + Анализ эффективности и рекомендации по улучшению",
    "Итерация 3: + Примеры использования и потенциальные баги"
]

for improvement in improvements:
    print(f"✓ {improvement}")

print("\n📊 ВЫВОДЫ:")
print("✅ Каждая итерация добавляет новый аспект анализа")
print("✅ Постепенное усложнение дает более полную картину")
print("✅ Финальный промпт (итерация 3) наиболее полезен")
print("💡 Ключевое улучшение: от простого к сложному")

# После всех итераций в задаче 5.2
print('\n' + '='*60)
print('ИТОГОВЫЙ АНАЛИЗ ЭВОЛЮЦИИ')
print('='*60)

evolution = [
    ("Итерация 0", "Базовый few-shot", "Негативный (распознал сарказм)"),
    ("Итерация 1", "+ Пример сарказма", "Негативный (та же классификация)"),
    ("Итерация 2", "+ Явная инструкция", "Сарказм (выделен в отдельный класс!) ✓"),
    ("Итерация 3", "+ Требование объяснения", "Сарказм + обоснование ✓")
]

for iter_name, change, result in evolution:
    print(f"\n{iter_name}: {change}")
    print(f"   Результат: {result}")

print("\n📊 КЛЮЧЕВЫЕ ВЫВОДЫ:")
print("✅ Явная инструкция критична для специфической классификации")
print("✅ Модель умная (распознала сарказм сразу), но нужно направить")
print("✅ Итерация 2 - минимум для правильной классификации")
print("✅ Итерация 3 - идеал (объяснение повышает прозрачность)")
```

---

### ✅✅ Часть 6: Комбинирование техник (18/20 баллов) → без изменений

**Отличная работа!** Эта часть выполнена лучше всех.

**Единственная проблема (−2 балла):**
- Few-shot пример слишком простой (показывает 4 шага вместо 7)

**Рекомендация по улучшению:**
Расширьте пример, чтобы показать все 7 шагов:
```python
example = """
Кандидат: Мария Иванова, Junior Developer, 2 года опыта
Вакансия: Senior Developer, требуется 5+ лет

Анализ:
Шаг 1: Опыт - 2 года, навыки - React, JavaScript
Шаг 2: Требования: 5+ лет Python, Django → несоответствие стека
Шаг 3: 5+ лет - НЕ соответствует, Python - НЕ соответствует
Шаг 4: Процент соответствия: 0%
Шаг 5: Сильные стороны: молодой специалист, растет
Шаг 6: Слабые стороны: недостаточно опыта, неподходящий стек
Шаг 7: Рекомендация - ОТКЛОНИТЬ (недостаточно опыта и несоответствие технологий)
"""
```

---

### 🎁 Бонусное задание (4/10 баллов) → без изменений

**Проблемы (не исправлены):**
- ❌ Отсутствует анализ точности на каждой итерации (−2 балла)
- ❌ **Не выполнен тест стабильности (5 запусков)** (−3 балла) - **КРИТИЧЕСКОЕ**
- ❌ Нет финального вывода о лучшем подходе (−1 балл)

**Это критическая часть бонусного задания!**

**Как исправить:**
```python
# 1. Золотой стандарт
golden = {
    news[0]: "ТЕХНОЛОГИИ",
    news[1]: "ЭКОНОМИКА",
    news[2]: "СПОРТ",
    news[3]: "НАУКА",
    news[4]: "ПОЛИТИКА",  # Спорный случай
    news[5]: "ТЕХНОЛОГИИ"
}

# 2. Функции для анализа
def extract_classification(text, news_list):
    """Извлекает классификацию из ответа"""
    classifications = {}
    for news_item in news_list:
        for category in ["ТЕХНОЛОГИИ", "СПОРТ", "ПОЛИТИКА", "ЭКОНОМИКА", "НАУКА"]:
            if news_item[:30] in text and category in text:
                classifications[news_item] = category
                break
    return classifications

def calculate_accuracy(predicted, golden):
    """Рассчитывает точность"""
    correct = sum(1 for k, v in predicted.items() if golden.get(k) == v)
    return correct / len(golden) * 100

# 3. Анализ каждой итерации
print('\n' + '='*70)
print('АНАЛИЗ ТОЧНОСТИ ПО ИТЕРАЦИЯМ')
print('='*70)

iteration_results = {}

for i, prompt_name in enumerate(prompt_dict):
    print(f"\n=== ИТЕРАЦИЯ {i}: {prompt_name} ===")
    response = get_response(prompt_dict[prompt_name])
    classifications = extract_classification(response, news)
    accuracy = calculate_accuracy(classifications, golden)
    iteration_results[prompt_name] = accuracy

    print(f"Точность: {accuracy:.0f}% ({int(accuracy/100*6)} из 6)")

    # Показать ошибки
    errors = [k for k, v in classifications.items() if golden.get(k) != v]
    if errors:
        print("❌ Ошибки:")
        for error in errors:
            print(f"   {error[:50]}...")
    else:
        print("✅ Все новости классифицированы правильно!")

# 4. КРИТИЧЕСКИ ВАЖНО: Тест стабильности
best_prompt = few_shot_3_sc  # Self-Consistency как лучший

print('\n' + '='*70)
print('ТЕСТ СТАБИЛЬНОСТИ (5 ЗАПУСКОВ ЛУЧШЕГО ПРОМПТА)')
print('='*70)

stability_results = []

for run in range(5):
    print(f"\n🔄 Запуск {run + 1}/5")
    response = get_response(best_prompt)
    classifications = extract_classification(response, news)
    accuracy = calculate_accuracy(classifications, golden)

    stability_results.append({
        'run': run + 1,
        'classifications': classifications,
        'accuracy': accuracy
    })

    print(f"   Точность: {accuracy:.0f}%")

# 5. Финальный анализ
print('\n' + '='*70)
print('ФИНАЛЬНЫЙ АНАЛИЗ')
print('='*70)

accuracies = [r['accuracy'] for r in stability_results]
avg_accuracy = sum(accuracies) / len(accuracies)

print(f"\n📊 Статистика стабильности:")
print(f"   Средняя точность: {avg_accuracy:.1f}%")
print(f"   Мин/Макс: {min(accuracies):.0f}% / {max(accuracies):.0f}%")

# Проверка идентичности
first = stability_results[0]['classifications']
all_same = all(r['classifications'] == first for r in stability_results)

if all_same:
    print("   ✅ Стабильность: 100% (все результаты идентичны)")
else:
    print("   ⚠️ Стабильность: есть вариации")

print(f"\n🏆 ФИНАЛЬНЫЙ ВЕРДИКТ:")
print(f"✅ Лучший подход: Few-shot + Self-Consistency")
print(f"✅ Точность: {avg_accuracy:.0f}%")
print(f"✅ Стабильность: {'Отличная' if all_same else 'Хорошая'}")

if avg_accuracy == 100 and all_same:
    print("✅ Промпт готов для продакшена!")
```

---

## Итоговые замечания

### 🎯 Что исправлено (+4 балла)

1. ✅ **Ошибки в категориях few-shot примеров** - все исправлены
   - One-shot: ПИТАНИЕ → ПРОДУКТЫ ✅
   - Few-shot: ПИТАНИЕ → ПРОДУКТЫ ✅
   - Диалог: ПИТАНИЕ → ПРОДУКТЫ ✅

### ⚠️ Критические проблемы (не исправлены)

**ГЛАВНАЯ ПРОБЛЕМА: По-прежнему отсутствуют разделы анализа!**

Это не просто "желательно иметь" - это **требование задания**. В задании четко указано:

- Часть 1.1: "Сравните результаты и определите..."
- Часть 1.2: "Сравните результат с текстовым few-shot подходом"
- Часть 2: "Сравните полноту и структурированность ответов"
- Часть 3: "Проверьте правильность рассуждений"
- Часть 4: "Запустите 3 раза и проверьте стабильность"
- Часть 5: "Сравнение результатов на каждом шаге"
- Бонус: **"Запустите лучший вариант 5 раз и оцените стабильность"**

**Влияние на оценку: −18 баллов**

### 📊 Оценка по частям (v2)

| Часть | Было | Стало | Изменение |
|-------|------|-------|-----------|
| Часть 1.1 | 6/10 | 8/10 | +2 ✅ |
| Часть 1.2 | 6/10 | 8/10 | +2 ✅ |
| Часть 2 | 7/10 | 7/10 | без изменений |
| Часть 3.1 | 7/10 | 7/10 | без изменений |
| Часть 3.2 | 8/10 | 8/10 | без изменений |
| Часть 4 | 7/10 | 7/10 | без изменений |
| Часть 5.1 | 7/10 | 7/10 | без изменений |
| Часть 5.2 | 8/10 | 8/10 | без изменений |
| Часть 6 | 18/20 | 18/20 | без изменений |
| Бонус | 4/10 | 4/10 | без изменений |
| **Итого** | **72/100** | **76/100** | **+4** |

### 💡 Приоритеты для следующей итерации

**Критический приоритет (обязательно):**

1. **Добавить анализ в Часть 1.1** (+2 балла)
   - Сравнение результатов трех подходов
   - Выводы о том, какой подход лучше

2. **Добавить сравнение в Часть 1.2** (+2 балла)
   - Сравнение текстового и диалогового few-shot

3. **Добавить сравнение в Часть 2** (+2 балла)
   - Анализ полноты multi-step vs простого промпта

4. **Добавить проверку правильности в Часть 3** (+3 балла)
   - Ручная проверка вычислений
   - Анализ преимуществ CoT

5. **Добавить извлечение результатов в Часть 4** (+2 балла)
   - Парсинг ответов из попыток
   - Анализ консенсуса

6. **Добавить анализ улучшений в Часть 5** (+3 балла)
   - Что улучшилось на каждой итерации
   - Итоговые выводы

7. **КРИТИЧЕСКИ ВАЖНО: Добавить тест стабильности в Бонус** (+4 балла)
   - 5 запусков лучшего промпта
   - Анализ стабильности
   - Финальные выводы

**С этими исправлениями оценка поднимется до: 76 + 18 = 94 балла!**

### 🎓 Общий вывод

**Текущее состояние:** Хорошо (76/100)

**Прогресс:** +4 балла за исправление категорий ✅

**Основная проблема:** Отсутствие аналитической части - это не технические ошибки, а непонимание того, что задание требует **не только выполнить код, но и проанализировать результаты**.

**Рекомендация:**

Вы исправили мелкие технические ошибки, что хорошо! Но основная проблема - **отсутствие анализа** - осталась нерешенной. Это не просто "дополнительные баллы", это **основное требование задания**.

Представьте, что вы - Data Scientist, и вам нужно не просто запустить модель, но и проанализировать, почему она работает так, а не иначе. Это критически важный навык!

**Следующие шаги:**
1. Добавьте разделы анализа в каждую часть (примеры кода выше)
2. Обязательно выполните тест стабильности для бонусного задания
3. Пересдача возможна с потенциальной оценкой до 94 баллов

Удачи! Вы на правильном пути, осталось добавить аналитическую составляющую! 🚀

---

**Связанные материалы:**
- [[Prompt_Engineering/Advanced_Prompt_Engineering_Strategies|Продвинутые стратегии Prompt Engineering]]
- [[Exercises/03_Prompt_Engineering_Review|Ревью задания №3]]
