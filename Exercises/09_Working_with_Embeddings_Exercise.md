# Практическое задание №9: Работа с Embeddings

## Цель задания

Освоить работу с векторными представлениями текста (embeddings): получение эмбеддингов через API, пакетную обработку, вычисление семантического сходства и визуализацию векторного пространства.

## Связанные материалы

- [[OpenAI_API/Working_with_Embeddings|Работа с Embeddings]]
- [[OpenAI_API/Working_with_OpenAI_API_in_Python|Работа с OpenAI API в Python]]
- [[LLMOps/Development_Phase|Фаза разработки — RAG и векторные базы данных]]

## Следующие шаги

После выполнения этого задания вы будете понимать основу RAG-систем и семантического поиска, что является продолжением тем, рассмотренных в [[Exercises/08_Function_Calling_Exercise|Практическом задании №8]].

## Предварительные требования

- Установленные библиотеки: `openai`, `python-dotenv`, `scipy`, `scikit-learn`, `numpy`, `matplotlib`
- Настроенный `.env` файл с API ключом
- Базовое понимание Python и линейной алгебры (векторы)

```python
# Установка зависимостей
# pip install openai python-dotenv scipy scikit-learn numpy matplotlib
```

---

## Часть 1: Первое знакомство с Embeddings API (15 минут)

### Задача 1.1: Получение эмбеддинга и изучение структуры ответа

**Что нужно сделать:**

1. Получите эмбеддинг для строки `"Искусственный интеллект меняет мир"`
2. Выведите:
   - Длину вектора (количество чисел)
   - Первые 5 чисел вектора
   - Тип данных каждого числа

**Ожидаемый результат:**
```
Длина вектора: 1536
Первые 5 чисел: [-0.021, 0.043, -0.018, 0.076, -0.031]
Тип: <class 'float'>
```

**Шаблон кода:**
```python
from openai import OpenAI
from dotenv import load_dotenv
import os

load_dotenv()

client = OpenAI(
    api_key=os.getenv("GEMINI_TOKEN"),
    base_url=os.getenv("BASE_URL")
)

text = "Искусственный интеллект меняет мир"

response = client.embeddings.create(
    model=os.getenv("BASE_MODEL"),
    input=text
)

response_dict = response.model_dump()

# Ваш код здесь
embedding = ...

print(f"Длина вектора: ...")
print(f"Первые 5 чисел: ...")
print(f"Тип: ...")
```

### Задача 1.2: Сравнение длин входных текстов

**Что нужно сделать:**

Получите эмбеддинги для трёх текстов разной длины и убедитесь, что длина вектора одинакова для всех:

```python
texts = [
    "Кот",
    "Кот сидит на диване",
    "Кот сидит на диване и смотрит в окно на падающий снег, мечтая о приключениях"
]
```

**Ожидаемый результат:**
```
"Кот" → длина вектора: 1536
"Кот сидит на диване" → длина вектора: 1536
"Кот сидит на диване и смотрит..." → длина вектора: 1536
Все длины одинаковы: True
```

**Шаблон кода:**
```python
texts = [
    "Кот",
    "Кот сидит на диване",
    "Кот сидит на диване и смотрит в окно на падающий снег, мечтая о приключениях"
]

# Ваш код здесь — получите эмбеддинги и сравните длины
```

---

## Часть 2: Пакетная обработка и вспомогательная функция (20 минут)

### Задача 2.1: Эмбеддинги для набора товаров

**Исходные данные:**
```python
products = [
    {"name": "Ноутбук MacBook Pro", "category": "Электроника"},
    {"name": "Беспроводные наушники Sony", "category": "Электроника"},
    {"name": "Кроссовки Nike Air Max", "category": "Обувь"},
    {"name": "Кожаные ботинки", "category": "Обувь"},
    {"name": "Роман «Мастер и Маргарита»", "category": "Книги"},
    {"name": "Учебник по машинному обучению", "category": "Книги"},
    {"name": "Смартфон Samsung Galaxy", "category": "Электроника"},
    {"name": "Кеды Converse", "category": "Обувь"},
]
```

**Что нужно сделать:**

1. Напишите функцию `create_embeddings(texts)`, которая:
   - Принимает строку или список строк
   - Возвращает список списков (всегда)
   - Делает **один** запрос к API для всего батча

2. Используя эту функцию, добавьте эмбеддинги каждому товару в словарь под ключом `"embedding"`

3. Выведите:
   - Количество товаров с эмбеддингами
   - Название первого товара и длину его вектора

**Ожидаемый результат:**
```
Товаров с эмбеддингами: 8
Первый товар: "Ноутбук MacBook Pro", длина вектора: 1536
```

**Шаблон кода:**
```python
products = [...]

def create_embeddings(texts):
    # Ваш код здесь
    pass

# Получите все эмбеддинги одним запросом
product_names = ...

embeddings = create_embeddings(product_names)

# Добавьте эмбеддинги в словари
for i, product in enumerate(products):
    product['embedding'] = ...

print(f"Товаров с эмбеддингами: ...")
print(f"Первый товар: \"{products[0]['name']}\", длина вектора: ...")
```

---

## Часть 3: Косинусное расстояние и семантический поиск (25 минут)

### Задача 3.1: Базовое вычисление сходства

**Что нужно сделать:**

Вычислите косинусное расстояние между следующими парами и объясните результаты:

| Пара | Ожидаемое сходство |
|------|-------------------|
| `"собака"` и `"пёс"` | Высокое (близкие слова) |
| `"автомобиль"` и `"машина"` | Высокое (синонимы) |
| `"кот"` и `"квантовая физика"` | Низкое (разные темы) |
| `"программирование"` и `"Python"` | Среднее-высокое (связанные темы) |

**Ожидаемый результат:**
```
"собака" ↔ "пёс": расстояние = 0.XXX (очень похожи)
"автомобиль" ↔ "машина": расстояние = 0.XXX (похожи)
"кот" ↔ "квантовая физика": расстояние = 0.XXX (не похожи)
"программирование" ↔ "Python": расстояние = 0.XXX (связаны)
```

**Шаблон кода:**
```python
from scipy.spatial import distance

pairs = [
    ("собака", "пёс"),
    ("автомобиль", "машина"),
    ("кот", "квантовая физика"),
    ("программирование", "Python"),
]

for text1, text2 in pairs:
    emb1 = create_embeddings(text1)[0]
    emb2 = create_embeddings(text2)[0]

    dist = ...

    print(f'"{text1}" ↔ "{text2}": расстояние = {dist:.4f}')
```

### Задача 3.2: Семантический поиск по каталогу товаров

**Используя данные из Задачи 2.1**, реализуйте семантический поиск:

**Что нужно сделать:**

1. Для каждого поискового запроса найдите **топ-3 наиболее похожих товара**
2. Выведите результаты с расстояниями

**Поисковые запросы:**
```python
queries = [
    "гаджет для музыки",
    "литература русских классиков",
    "спортивная обувь для бега",
]
```

**Ожидаемый результат:**
```
Запрос: "гаджет для музыки"
  1. Беспроводные наушники Sony (dist=0.XXX)
  2. Смартфон Samsung Galaxy (dist=0.XXX)
  3. Ноутбук MacBook Pro (dist=0.XXX)

Запрос: "литература русских классиков"
  1. Роман «Мастер и Маргарита» (dist=0.XXX)
  ...
```

**Шаблон кода:**
```python
import numpy as np

queries = [
    "гаджет для музыки",
    "литература русских классиков",
    "спортивная обувь для бега",
]

for query in queries:
    query_embedding = create_embeddings(query)[0]

    distances = []
    for product in products:
        dist = distance.cosine(query_embedding, product['embedding'])
        distances.append(dist)

    # Получите индексы топ-3 ближайших товаров
    top3_indices = ...

    print(f"Запрос: \"{query}\"")
    for rank, idx in enumerate(top3_indices, 1):
        print(f"  {rank}. {products[idx]['name']} (dist={distances[idx]:.4f})")
    print()
```

---

## Часть 4: Визуализация векторного пространства (20 минут)

### Задача 4.1: t-SNE визуализация каталога товаров

**Используя данные из Задачи 2.1**, визуализируйте эмбеддинги:

**Что нужно сделать:**

1. Примените t-SNE для снижения размерности до 2D
2. Постройте scatter plot, где:
   - Каждая точка — товар
   - Точки одной категории — одного цвета
   - Подписи — названия категорий (не товаров)
3. Добавьте заголовок и легенду

**Ожидаемый результат:** График, на котором товары одной категории (Электроника, Обувь, Книги) группируются вместе.

**Шаблон кода:**
```python
from sklearn.manifold import TSNE
import numpy as np
import matplotlib.pyplot as plt

embeddings_matrix = np.array([product['embedding'] for product in products])

# perplexity должна быть меньше числа точек данных (у нас 8 точек)
tsne = TSNE(n_components=2, perplexity=...)
embeddings_2d = tsne.fit_transform(embeddings_matrix)

# Цвета для категорий
category_colors = {
    "Электроника": "blue",
    "Обувь": "green",
    "Книги": "red",
}

plt.figure(figsize=(10, 7))

# Ваш код здесь: нарисуйте точки разными цветами по категориям
# и добавьте аннотации с категориями

plt.title("...")
plt.legend()
plt.show()
```

---

## Часть 5: Автоклассификация по категориям (20 минут)

### Задача 5.1: Классификация новых товаров без обучения

**Сценарий:** В интернет-магазин добавляют новые товары. Нужно автоматически определить их категорию по семантическому сходству с уже известными товарами из каталога.

**Новые товары для классификации:**
```python
new_items = [
    "Планшет iPad",
    "Сапоги зимние женские",
    "Детектив Агаты Кристи",
    "Наушники JBL",
    "Кроссовки Adidas",
]
```

**Что нужно сделать:**

1. Для каждого нового товара найдите наиболее похожий товар из каталога
2. Присвойте новому товару категорию найденного ближайшего соседа
3. Выведите результат классификации

**Ожидаемый результат:**
```
"Планшет iPad" → Электроника (ближайший: "Смартфон Samsung Galaxy", dist=0.XXX)
"Сапоги зимние женские" → Обувь (ближайший: "Кожаные ботинки", dist=0.XXX)
"Детектив Агаты Кристи" → Книги (ближайший: "Роман «Мастер и Маргарита»", dist=0.XXX)
"Наушники JBL" → Электроника (ближайший: "Беспроводные наушники Sony", dist=0.XXX)
"Кроссовки Adidas" → Обувь (ближайший: "Кроссовки Nike Air Max", dist=0.XXX)
```

**Шаблон кода:**
```python
new_items = [
    "Планшет iPad",
    "Сапоги зимние женские",
    "Детектив Агаты Кристи",
    "Наушники JBL",
    "Кроссовки Adidas",
]

new_embeddings = create_embeddings(new_items)

for item_name, item_emb in zip(new_items, new_embeddings):
    distances = []
    for product in products:
        dist = distance.cosine(item_emb, product['embedding'])
        distances.append(dist)

    nearest_idx = np.argmin(distances)
    nearest_product = products[nearest_idx]

    # Ваш код здесь: выведите категорию и ближайший товар
    print(...)
```

---

## Бонусное задание (необязательно, +20 минут)

### Задача 6: Построение мини-семантической поисковой системы

**Сценарий:** Вы строите поиск по базе знаний FAQ службы поддержки.

**База знаний:**
```python
faq = [
    {"question": "Как отменить заказ?", "answer": "Зайдите в раздел 'Мои заказы' и нажмите 'Отменить'."},
    {"question": "Когда придёт моя посылка?", "answer": "Срок доставки 2-5 рабочих дней."},
    {"question": "Как вернуть товар?", "answer": "Возврат возможен в течение 14 дней с момента получения."},
    {"question": "Как изменить адрес доставки?", "answer": "Свяжитесь с поддержкой до момента отправки заказа."},
    {"question": "Принимаете ли оплату картой?", "answer": "Да, принимаем Visa, MasterCard и МИР."},
    {"question": "Есть ли у вас программа лояльности?", "answer": "Да, при каждой покупке начисляются бонусные баллы."},
]
```

**Что нужно сделать:**

1. Получите эмбеддинги для всех вопросов
2. Реализуйте функцию `find_answer(user_query, faq, threshold=0.5)`:
   - Если минимальное расстояние меньше `threshold` — вернуть найденный ответ
   - Иначе — вернуть `"Извините, не нашли ответ на ваш вопрос."`
3. Протестируйте с запросами пользователей:

```python
user_queries = [
    "Хочу отказаться от покупки",           # → отмена заказа
    "Сколько ждать доставку?",               # → срок доставки
    "Могу ли я расплатиться Visa?",          # → оплата картой
    "Как приготовить борщ?",                 # → не найдено
]
```

**Ожидаемый результат:**
```
Запрос: "Хочу отказаться от покупки"
Ответ: Зайдите в раздел 'Мои заказы' и нажмите 'Отменить'.

Запрос: "Как приготовить борщ?"
Ответ: Извините, не нашли ответ на ваш вопрос.
```

---

## Критерии оценки

| Часть | Баллы | Критерий |
|-------|-------|----------|
| Часть 1 | 10 | Корректное получение эмбеддингов и изучение структуры ответа |
| Часть 2 | 20 | Реализация пакетной обработки и функции `create_embeddings` |
| Часть 3 | 25 | Вычисление сходства и реализация топ-3 семантического поиска |
| Часть 4 | 20 | Визуализация t-SNE с цветами по категориям и легендой |
| Часть 5 | 25 | Автоклассификация по ближайшему соседу с корректными результатами |
| Бонус | 20 | Семантическая поисковая система с порогом и fallback-ответом |
| **Всего** | **100** | |

## Рекомендации по выполнению

1. **Выполняйте последовательно**: каждая часть использует результаты предыдущей — функция `create_embeddings` из Части 2 нужна во всех последующих частях
2. **Экономьте токены**: при отладке используйте одну короткую строку, а не весь батч
3. **Обращайте внимание на расстояния**: значение косинусного расстояния от 0 до 2, где меньше — ближе
4. **Для t-SNE**: `perplexity` должна быть строго меньше числа точек данных
5. **Сохраняйте эмбеддинги**: не делайте повторные запросы к API без необходимости — сохраните результаты в переменные

## Что вы освоите

После выполнения этого задания вы сможете:
- Получать векторные представления текста через Embeddings API
- Применять пакетную обработку для эффективной работы с API
- Вычислять семантическое сходство с помощью косинусного расстояния
- Реализовывать семантический поиск и классификацию без обучения
- Визуализировать многомерные данные с помощью t-SNE
- Строить простейшие RAG-компоненты для поиска по базе знаний

---

**Удачи в выполнении задания!**

*Если возникнут вопросы, обратитесь к материалам [[OpenAI_API/Working_with_Embeddings|Работа с Embeddings]]*
