# Практическое задание №1: Обработка текста с OpenAI API

## Цель задания

Отработать на практике основные техники работы с OpenAI API: редактирование текста, резюмирование, управление параметрами генерации, расчет стоимости и использование shot prompting.

## Связанные материалы

- [[OpenAI_API/Working_with_OpenAI_API_in_Python|Работа с OpenAI API в Python]]
- [[OpenAI_API/Introduction_to_OpenAI_API|Введение в OpenAI API]]

## Предварительные требования

- Установленная библиотека `openai` и `python-dotenv`
- Настроенный `.env` файл с API ключом
- Базовое понимание Python

---

## Часть 1: Редактирование текста (15 минут)

### Задача 1.1: Многоязычная трансформация

**Исходный текст:**
```
Sarah Johnson is a Senior Data Scientist at TechCorp. She specializes in machine learning and has 8 years of experience. Her favorite programming language is Python.
```

**Что нужно сделать:**

1. Создайте промпт, который:
   - Переведет текст на русский язык
   - Изменит имя на "Алексей Петров"
   - Изменит должность на "Ведущий Аналитик Данных"
   - Изменит опыт на 5 лет
   - Изменит язык программирования на R

2. Отправьте запрос к API и выведите результат

**Ожидаемый результат:**
```
Алексей Петров является Ведущим Аналитиком Данных в компании TechCorp. Он специализируется на машинном обучении и имеет 5 лет опыта. Его любимый язык программирования R.
```

**Код для проверки:**
```python
from openai import OpenAI
from dotenv import load_dotenv
import os

load_dotenv()

client = OpenAI(
    api_key=os.getenv("GEMINI_TOKEN"),
    base_url=os.getenv("BASE_URL")
)

# Ваш код здесь
prompt = """..."""

response = client.chat.completions.create(
    model=os.getenv("BASE_MODEL"),
    messages=[{"role": "user", "content": prompt}]
)

print(response.choices[0].message.content)
```

---

## Часть 2: Резюмирование и расчет стоимости (20 минут)

### Задача 2.1: Анализ отзывов клиентов

**Исходные данные:**
```python
reviews = """
Отзыв 1: Заказал ноутбук через ваш интернет-магазин. Доставка заняла 3 дня вместо обещанных 2, но курьер был очень вежливым. Ноутбук пришел в отличной упаковке, все работает прекрасно. Немного расстроила задержка, но в целом доволен покупкой.

Отзыв 2: Это просто ужас! Заказывал смартфон, пришел с царапиной на экране. Звонил в поддержку три раза, никто не перезвонил. Верну товар и больше никогда здесь ничего не закажу.

Отзыв 3: Отличный магазин! Заказал наушники, пришли на следующий день. Качество супер, цена ниже чем в других магазинах. Менеджер Анна помогла с выбором, очень внимательная. Буду заказывать еще!

Отзыв 4: Заказ пришел вовремя, товар соответствует описанию. Ничего особенного, обычный интернет-магазин. Цены средние.
"""
```

**Что нужно сделать:**

1. Создайте промпт для резюмирования отзывов в следующем формате:
   - Общее настроение (положительное/нейтральное/отрицательное)
   - Главные проблемы (если есть)
   - Главные преимущества (если есть)
   - Рекомендации для улучшения сервиса

2. Установите `max_completion_tokens = 300`

3. Рассчитайте стоимость запроса, используя следующие цены:
   - Входные токены: $0.15 за 1,000,000 токенов
   - Выходные токены: $0.60 за 1,000,000 токенов

4. Выведите:
   - Резюме отзывов
   - Количество использованных входных токенов
   - Количество использованных выходных токенов (фактическое, не max)
   - Реальную стоимость запроса
   - Максимально возможную стоимость (если бы использовались все 300 токенов)

**Ожидаемый результат (примерный):**
```
=== РЕЗЮМЕ ОТЗЫВОВ ===
[Текст резюме от модели]

=== СТАТИСТИКА ===
Входные токены: XXX
Выходные токены: XXX
Реальная стоимость: $0.000XXXXX
Максимальная стоимость: $0.000XXXXX
```

**Шаблон кода:**
```python
reviews = """..."""

prompt = f"""..."""

max_completion_tokens = 300

response = client.chat.completions.create(
    model=os.getenv("BASE_MODEL"),
    messages=[{"role": "user", "content": prompt}],
    max_completion_tokens=max_completion_tokens
)

# Расчет стоимости
input_token_price = 0.15 / 1000000
output_token_price = 0.60 / 1000000

input_tokens = response.usage.prompt_tokens
output_tokens = response.usage.completion_tokens  # Фактическое количество

real_cost = (input_tokens * input_token_price) + (output_tokens * output_token_price)
max_cost = (input_tokens * input_token_price) + (max_completion_tokens * output_token_price)

# Вывод результатов
print("=== РЕЗЮМЕ ОТЗЫВОВ ===")
print(response.choices[0].message.content)
print("\n=== СТАТИСТИКА ===")
print(f"Входные токены: {input_tokens}")
print(f"Выходные токены: {output_tokens}")
print(f"Реальная стоимость: ${real_cost:.8f}")
print(f"Максимальная стоимость: ${max_cost:.8f}")
```

---

## Часть 3: Генерация текста с управлением температурой (15 минут)

### Задача 3.1: Эксперимент с temperature

**Что нужно сделать:**

1. Создайте промпт для генерации слогана нового кафе "Уютный уголок", которое специализируется на домашней выпечке и кофе

2. Отправьте **3 идентичных запроса** с разными значениями `temperature`:
   - temperature = 0 (детерминированный)
   - temperature = 1 (сбалансированный)
   - temperature = 2 (креативный)

3. Сравните результаты и сделайте выводы

**Ожидаемый результат:**
```
=== ТЕМПЕРАТУРА: 0 ===
[Слоган 1]

=== ТЕМПЕРАТУРА: 1 ===
[Слоган 2]

=== ТЕМПЕРАТУРА: 2 ===
[Слоган 3]

=== ВЫВОДЫ ===
[Ваши наблюдения о различиях]
```

**Шаблон кода:**
```python
prompt = """..."""

temperatures = [0, 1, 2]

for temp in temperatures:
    response = client.chat.completions.create(
        model=os.getenv("BASE_MODEL"),
        messages=[{"role": "user", "content": prompt}],
        temperature=temp
    )

    print(f"=== ТЕМПЕРАТУРА: {temp} ===")
    print(response.choices[0].message.content)
    print()

# Ваши выводы
print("=== ВЫВОДЫ ===")
print("...")
```

---

## Часть 4: Shot Prompting для классификации (25 минут)

### Задача 4.1: Классификация уровня срочности заявок

**Исходные данные:**
```python
tickets = [
    "Сервер полностью недоступен, клиенты не могут зайти на сайт",
    "Хотелось бы добавить кнопку экспорта в Excel в отчет",
    "База данных упала, транзакции не проходят",
    "Можно ли изменить цвет кнопки на главной странице?",
    "Пользователи не могут сбросить пароль, ошибка 500",
    "Предлагаю добавить темную тему в приложение"
]
```

**Что нужно сделать:**

1. **Zero-shot подход**:
   - Создайте промпт для классификации заявок на три уровня: КРИТИЧЕСКИЙ, СРЕДНИЙ, НИЗКИЙ
   - Классифицируйте все 6 заявок
   - Сохраните результаты

2. **Few-shot подход**:
   - Создайте промпт с 3 примерами (по одному для каждого уровня)
   - Классифицируйте те же 6 заявок
   - Сохраните результаты

3. **Сравните результаты** двух подходов

**Примеры для few-shot:**
```
- Принтер на третьем этаже не печатает - НИЗКИЙ
- Система оплаты не принимает карты - СРЕДНИЙ
- Произошла утечка данных клиентов - КРИТИЧЕСКИЙ
```

**Ожидаемый результат:**
```
=== ZERO-SHOT КЛАССИФИКАЦИЯ ===
1. "Сервер полностью недоступен..." - [уровень]
2. "Хотелось бы добавить кнопку..." - [уровень]
...

=== FEW-SHOT КЛАССИФИКАЦИЯ ===
1. "Сервер полностью недоступен..." - [уровень]
2. "Хотелось бы добавить кнопку..." - [уровень]
...

=== СРАВНЕНИЕ ===
Совпадений: X из 6
Различия:
- Заявка №X: zero-shot=[уровень], few-shot=[уровень]
```

**Шаблон кода:**
```python
tickets = [...]

# Zero-shot
zero_shot_prompt = """Классифицируй следующие заявки в техподдержку на три уровня срочности:
КРИТИЧЕСКИЙ - требует немедленного решения, влияет на бизнес
СРЕДНИЙ - важная проблема, но есть временное решение
НИЗКИЙ - улучшения и некритичные запросы

Заявки:
1. ...
2. ...
"""

response_zero = client.chat.completions.create(
    model=os.getenv("BASE_MODEL"),
    messages=[{"role": "user", "content": zero_shot_prompt}]
)

print("=== ZERO-SHOT КЛАССИФИКАЦИЯ ===")
print(response_zero.choices[0].message.content)

# Few-shot
few_shot_prompt = """Классифицируй следующие заявки в техподдержку на три уровня срочности:

Примеры:
- Принтер на третьем этаже не печатает - НИЗКИЙ
- Система оплаты не принимает карты - СРЕДНИЙ
- Произошла утечка данных клиентов - КРИТИЧЕСКИЙ

Заявки:
1. ...
2. ...
"""

response_few = client.chat.completions.create(
    model=os.getenv("BASE_MODEL"),
    messages=[{"role": "user", "content": few_shot_prompt}]
)

print("\n=== FEW-SHOT КЛАССИФИКАЦИЯ ===")
print(response_few.choices[0].message.content)

# Сравнение результатов - ваш код
```

---

## Бонусное задание (необязательно, +20 минут)

### Задача 5: Комплексная обработка данных

**Сценарий**: Вы работаете в компании, которая получает отзывы о курсах на разных языках.

**Что нужно сделать:**

1. Возьмите отзыв на английском языке
2. Переведите его на русский (используя редактирование текста)
3. Классифицируйте настроение от 1 до 5 (используя few-shot)
4. Создайте краткое резюме отзыва (2-3 предложения)
5. Сгенерируйте вежливый ответ от лица компании
6. Рассчитайте общую стоимость всех запросов

**Исходный отзыв:**
```
The Python course was absolutely amazing! The instructor explained complex topics like decorators and generators in a very simple way. However, I found the final project a bit too challenging and would have appreciated more guidance. Overall, highly recommend this course for intermediate learners.
```

**Ожидаемый результат:**
```
=== ШАГ 1: ПЕРЕВОД НА РУССКИЙ ===
[Переведенный текст]

=== ШАГ 2: ОЦЕНКА НАСТРОЕНИЯ ===
Оценка: X/5

=== ШАГ 3: РЕЗЮМЕ ===
[Краткое резюме]

=== ШАГ 4: ОТВЕТ ОТ КОМПАНИИ ===
[Сгенерированный ответ]

=== ШАГ 5: ОБЩАЯ СТОИМОСТЬ ===
Всего запросов: 4
Входные токены: XXX
Выходные токены: XXX
Общая стоимость: $0.000XXXXX
```

---

## Критерии оценки

| Часть | Баллы | Критерий |
|-------|-------|----------|
| Часть 1 | 15 | Корректное редактирование текста с учетом всех требований |
| Часть 2 | 25 | Качественное резюмирование + правильный расчет стоимости |
| Часть 3 | 15 | Эксперимент с temperature + анализ результатов |
| Часть 4 | 30 | Реализация zero-shot и few-shot + сравнение |
| Часть 5 | 15 | Комплексная обработка с расчетом общей стоимости |
| **Всего** | **100** | |

## Рекомендации по выполнению

1. **Начните с простого**: Выполняйте задания последовательно
2. **Экспериментируйте с промптами**: Если результат не устраивает, попробуйте переформулировать промпт
3. **Сохраняйте код**: Создайте отдельный Python файл или Jupyter notebook для каждой части
4. **Анализируйте результаты**: Обращайте внимание на то, как разные параметры влияют на ответы модели
5. **Документируйте выводы**: Записывайте наблюдения - это поможет в будущей работе с API

## Что вы освоите

После выполнения этого задания вы сможете:
- ✅ Эффективно редактировать и трансформировать тексты через API
- ✅ Резюмировать большие объемы текстовой информации
- ✅ Рассчитывать и оптимизировать стоимость запросов к API
- ✅ Управлять креативностью и детерминированностью ответов через temperature
- ✅ Применять zero-shot и few-shot промптинг для задач классификации
- ✅ Строить комплексные пайплайны обработки текста

---

**Удачи в выполнении задания! 🚀**

*Если возникнут вопросы, обратитесь к материалам [[OpenAI_API/Working_with_OpenAI_API_in_Python|Работа с OpenAI API в Python]]*
